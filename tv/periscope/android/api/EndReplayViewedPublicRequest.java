package tv.periscope.android.api;

import defpackage.op;

/* compiled from: Twttr */
public class EndReplayViewedPublicRequest extends PublicRequest {
    @op(a = "broadcast_id")
    public String broadcastId;
    @op(a = "session")
    public String session;
}
